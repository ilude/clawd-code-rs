// Replace occurrences of 'Claude' with 'Clawd' in the source code

// Example JavaScript code
const example = 'Claude is a great AI!';
const modifiedExample = example.replace(/Claude/g, 'Clawd').replace(/claude/g, 'clawd');
console.log(modifiedExample);